{
	"name": "docker-proxy",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"wrangler": "wrangler dev",
		"deno": "deno run --allow-net --allow-env src/deno.ts"
	},
	"devDependencies": {
		"wrangler": "^4.75.0",
		"deno": "^2.2.0"
	}
}
